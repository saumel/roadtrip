# Put your family photo here 📸

Save the attached hammock family photo in this folder and name it exactly:

    family.jpg

That's it — the home page hero will automatically use it.

If the file is missing, the site still looks great with a tropical gradient fallback.
Other accepted approach: keep the name `family.jpg` (lowercase, .jpg extension).
